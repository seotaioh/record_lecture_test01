# 강의 녹음기 사용 지침

## 1. 응용 프로그램 실행

macOS의 Spotlight에서 `강의 녹음기`를 검색하거나 Finder의 `응용 프로그램`에서
`강의 녹음기.app`을 실행합니다.

앱은 이 저장소의 `recorder_gui.py`를 직접 실행합니다. 소스를 수정한 경우 실행 중인
앱을 완전히 종료하고 다시 실행하면 변경 사항이 적용됩니다.

> 프로젝트 폴더를 다른 위치로 옮기면 `/Applications/강의 녹음기.app`의 실행 경로도
> 다시 설정해야 합니다.

## 2. 저장 폴더 구조

저장소의 상위 작업 폴더에 다음 구조를 사용합니다.

```text
2) 코텍스_강의_녹음_캡처/
├── 1) 녹음/
│   └── 녹음1) YYYY-MM-DD/
├── 2) 전사/
│   └── 전사2) YYYY-MM-DD/
├── 3) 캡처/
│   └── 캡처3) YYYY-MM-DD/
├── 4) 종합폴더/
│   └── 종합4) YYYY-MM-DD/
└── record_lecture_test01/
```

- 녹음: 16kHz 모노 WAV 파일만 날짜 폴더에 직접 저장합니다.
- 전사: Whisper TXT 파일을 날짜 폴더에 저장합니다.
- 캡처: 외장 화면 2번의 PNG와 PDF를 날짜 폴더에 저장합니다.
- 각 번호 폴더 바로 아래에는 날짜 폴더만 사용합니다.

`4) 종합폴더로 모으기` 버튼을 누르면 오늘 날짜의 녹음 WAV, 전사 TXT,
캡처 PNG와 PDF를 `4) 종합폴더/종합4) YYYY-MM-DD/` 한곳으로 이동합니다.
이동이 끝나면 `1) 녹음`, `2) 전사`, `3) 캡처`의 날짜 폴더는 빈 상태로 남습니다.
파일명 충돌이나 이동 오류가 발생하면 이동을 중단하거나 이미 이동한 파일을 원래 위치로
되돌립니다.

## 3. 녹음

1. macOS 시스템 출력을 BlackHole이 포함된 다중 출력 장치로 변경합니다.
2. 앱에서 `녹음 시작`을 누릅니다.
3. 소리가 감지되면 자동으로 녹음하고, 일정 시간 조용하면 WAV를 저장합니다.
4. 작업이 끝나면 `녹음 정지`를 누릅니다.

저장 형식은 전사용 16kHz 모노 WAV이며 48kHz 원본은 만들지 않습니다.
강의 소리가 10분 동안 계속 무음일 때만 자동 저장하므로 짧은 휴식에는 파일이 분할되지 않습니다.

현재 출력이 AndroidTV, MacBook 스피커 등 단일 장치이면 BlackHole 입력이 무음이므로
앱이 녹음 시작을 차단하고 사운드 설정을 엽니다. 반드시 BlackHole이 포함된
`다중 출력 기기`를 선택한 뒤 녹음을 시작합니다.

이미 여러 파일로 나뉜 경우 녹음을 정지한 뒤 `분할 음성 합치기`를 누릅니다.
확인창에서 승인하면 오늘 날짜의 분할 WAV를 시간순으로 합치고, 합본의 길이와 형식을
검증한 뒤에만 기존 조각 파일을 삭제합니다.

## 4. 자동 전사

`녹음 후 자동 전사`를 선택하면 저장된 WAV를 로컬 Whisper로 전사합니다.
결과 TXT는 `2) 전사/전사2) YYYY-MM-DD/`에 저장됩니다.

최초 실행 시 Whisper 모델 다운로드로 시간이 걸릴 수 있습니다.

전사가 여러 TXT로 나뉜 경우 `TXT 전사 파일 합치기` 버튼을 누르면 오늘 날짜의
개별 전사를 파일명 시간순으로 이어 `강의_전사_합본_YYYY-MM-DD_HH-MM-SS.txt`를
만듭니다. 합본 내용을 검증한 뒤 기존 개별 TXT는 자동으로 삭제합니다.

## 5. 외장 화면 캡처

`외장 화면 캡처`를 누르면 화면 2번 전체를 PNG로 저장합니다.

최초 실행 시 macOS가 화면 기록 권한을 요청합니다. `시스템 설정 → 개인정보 보호 및 보안
→ 화면 및 시스템 오디오 기록`에서 `강의 녹음기`를 켠 뒤 앱을 완전히 종료하고 다시
실행해야 합니다.

```text
3) 캡처/캡처3) YYYY-MM-DD/화면_YYYY-MM-DD_HH-MM-SS-ffffff.png
```

앱 창은 다른 앱 뒤로 숨지 않도록 항상 위에 표시됩니다.

## 6. PNG 정리 후 PDF 만들기

PDF는 캡처와 동시에 자동 생성되지 않습니다.

1. 필요한 화면을 PNG로 캡처합니다.
2. `저장 폴더들 열기`를 누릅니다.
3. 잘못 캡처한 PNG를 Finder에서 삭제합니다.
4. 앱의 `남은 PNG를 PDF로 묶기`를 누릅니다.
5. 남은 PNG만 시간순으로 정렬해 날짜별 PDF를 새로 만듭니다.

```text
3) 캡처/캡처3) YYYY-MM-DD/강의캡처_YYYY-MM-DD.pdf
```

변환 버튼을 다시 누르면 현재 남아 있는 PNG 목록을 기준으로 기존 PDF를 다시 만듭니다.
PNG 원본은 삭제하지 않습니다.

## 7. 터미널에서 직접 실행

```bash
cd "/Users/stosgh/Desktop/10)  클로드 코드 /2) 코텍스_강의_녹음_캡처/record_lecture_test01"
source .venv/bin/activate
python recorder_gui.py
```

필요 패키지를 다시 설치하려면 다음을 실행합니다.

```bash
python -m pip install -r requirements.txt
```

## 8. 수정 및 GitHub 보관

소스 수정 후 다음 순서로 확인합니다.

```bash
python -m py_compile recorder.py recorder_gui.py transcribe.py
git diff --check
git status
```

검사가 통과하면 변경 내용을 커밋하고 GitHub에 올립니다.

```bash
git add README.md 사용_지침.md recorder.py recorder_gui.py requirements.txt
git commit -m "Update lecture recorder workflow"
git push origin main
```

## 9. 주의 사항

- 강의 녹음과 캡처는 개인 학습 및 복습 용도로만 사용합니다.
- 강의 플랫폼의 이용약관과 저작권을 확인합니다.
- 같은 파일을 데스크톱 Codex와 터미널 Codex에서 동시에 수정하지 않습니다.
- 데스크톱 동기화 오류 표시가 있으면 중요한 자료를 별도로 백업합니다.
